# Frontend scaffold

This is a minimal scaffold for the desktop-like UI using Electron + React + Monaco.
The full UI will implement a left-side repo explorer, a center Monaco editor, and
a right-side AI assistant panel with chat, multi-queue controls and admin approvals.

To run locally (after installing node and npm):

cd frontend
npm install
npm start
